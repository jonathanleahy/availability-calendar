import React from 'react';
import PropTypes from 'prop-types';

const Diary = props => {
    return (
        <div>
            Hello
        </div>
    );
};

Diary.propTypes = {

};

export default Diary;
